<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <title>Relieving order / Service certificate content</title>
    <link href="https://fonts.googleapis.com/css?family=Montserrat:300,400,500,700" rel="stylesheet">
</head>

<body style="font-family: 'Poppins' , sans-serif; font-size:14px;color:#302c2b; line-height:2;">
    <div class="rt-tb-scrl">
        <div class="col-md-12">
            <p>Dear Sir / Madam,</p>
            @if (Model.RelievingMode != 3 && Model.subject.Contains("Relieving order / Service certificate"))
            {
                <p>Please find attached the relieving order and service certificate of <b>@Model.PersonName, @Model.DesignationName</b> relieved on <b>@Model.AppointmentStartDate</b> from the project no : <b>@Model.ProjectNumber</b>.</p>
            }
            else if (Model.RelievingMode != 3 && Model.subject.Contains("Relieving order"))
            {
                <p>Please find attached the relieving order of <b>@Model.PersonName, @Model.DesignationName</b> relieved on <b>@Model.AppointmentStartDate</b> from the project no : <b>@Model.ProjectNumber</b>.</p>
            }
            else if (Model.RelievingMode != 3 && Model.subject.Contains("Service certificate"))
            {
                <p>Please find attached the service certificate of <b>@Model.PersonName, @Model.DesignationName</b> relieved on <b>@Model.AppointmentStartDate</b> from the project no : <b>@Model.ProjectNumber</b>.</p>
            }
            else
            {
                <p>Please find attached the relieving order of <b>@Model.PersonName, @Model.DesignationName</b>  relieved on <b>@Model.AppointmentStartDate</b> from the project no : <b>@Model.ProjectNumber</b></p>
            }            
            <p>For any queries, kindly send an email to <a href="mailto:icsrrecruitment4@imail.iitm.ac.in" target="_blank"> icsrrecruitment4@imail.iitm.ac.in </a>/<a href="mailto:icsrrecruitment@iitm.ac.in" target="_blank"> icsrrecruitment@iitm.ac.in</a> and we will get back to you at the earliest.</p>
            <p>Thanks and Regards,</p>
            <p style="margin-top:0px;">
                @Model.DAName<br>
                Project Recruitment<br>
                Centre for IC&SR<br>
                IIT Madras<br>
                *This is a system generated email.
            </p>
        </div>
    </div>
</body>
</html>